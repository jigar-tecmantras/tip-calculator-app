# Tip Calculator App

A small React-based tip calculator that lets users enter the bill amount, select a tip percentage (including preset buttons), choose how many people are sharing the check, and instantly see the total tip and per-person split.

## Setup

```bash
cd frontend
npm install
npm start
```
